// Code generated by ent, DO NOT EDIT.

package repo

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Alijeyrad/simorq_backend/internal/repo/internprofile"
	"github.com/google/uuid"
)

// InternProfile is the model entity for the InternProfile schema.
type InternProfile struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// FK → clinic_members.id
	ClinicMemberID uuid.UUID `json:"clinic_member_id,omitempty"`
	// InternshipYear holds the value of the "internship_year" field.
	InternshipYear *int `json:"internship_year,omitempty"`
	// List of clinic_member UUIDs who supervise this intern
	SupervisorIds []uuid.UUID `json:"supervisor_ids,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InternProfile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case internprofile.FieldSupervisorIds:
			values[i] = new([]byte)
		case internprofile.FieldInternshipYear:
			values[i] = new(sql.NullInt64)
		case internprofile.FieldCreatedAt, internprofile.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case internprofile.FieldID, internprofile.FieldClinicMemberID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InternProfile fields.
func (_m *InternProfile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case internprofile.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case internprofile.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case internprofile.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case internprofile.FieldClinicMemberID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field clinic_member_id", values[i])
			} else if value != nil {
				_m.ClinicMemberID = *value
			}
		case internprofile.FieldInternshipYear:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field internship_year", values[i])
			} else if value.Valid {
				_m.InternshipYear = new(int)
				*_m.InternshipYear = int(value.Int64)
			}
		case internprofile.FieldSupervisorIds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field supervisor_ids", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.SupervisorIds); err != nil {
					return fmt.Errorf("unmarshal field supervisor_ids: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the InternProfile.
// This includes values selected through modifiers, order, etc.
func (_m *InternProfile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this InternProfile.
// Note that you need to call InternProfile.Unwrap() before calling this method if this InternProfile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *InternProfile) Update() *InternProfileUpdateOne {
	return NewInternProfileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the InternProfile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *InternProfile) Unwrap() *InternProfile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("repo: InternProfile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *InternProfile) String() string {
	var builder strings.Builder
	builder.WriteString("InternProfile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("clinic_member_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ClinicMemberID))
	builder.WriteString(", ")
	if v := _m.InternshipYear; v != nil {
		builder.WriteString("internship_year=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("supervisor_ids=")
	builder.WriteString(fmt.Sprintf("%v", _m.SupervisorIds))
	builder.WriteByte(')')
	return builder.String()
}

// InternProfiles is a parsable slice of InternProfile.
type InternProfiles []*InternProfile
